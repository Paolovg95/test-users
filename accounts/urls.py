from .views import account_view, signup
from django.urls import path, include

urlpatterns = [
    # path('', account_view, name="my_account"),
    # path('signup/', signup, name="signup_url"),
    # path('login/', login, name="login_url"),
    # path('confirm-email/', email_confirmation, name="email_confirmation_url"),
    # path('company-invitation/', accept_invitation, name="accept_invitation"),
    # path('company-invitation/<str:company>/', create_invitation, name="company_invitation"),
]
